# Tmux loads ~/.tmux.conf before this XDG path. Source the shared config only
# when a tool, such as Omarchy's keybinding menu, loads this file in isolation.
if-shell -F '#{!=:#{@thevetat-config-loaded},1}' 'source-file ~/.tmux.conf'
